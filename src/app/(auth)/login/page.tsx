'use client'

import React from 'react';
import AuthForm from "@/components/global/auth-form";

const LoginPage = () => {
    return (
        <AuthForm type={'login'}/>
    )
};

export default LoginPage;